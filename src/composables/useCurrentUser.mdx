import {Meta} from '@storybook/addon-docs/blocks';

<Meta title="Composables/useCurrentUser" />

# useCurrentUser

Collection of helper functions to get details about the current user and calculate submission related permissions.
