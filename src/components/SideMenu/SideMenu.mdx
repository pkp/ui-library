import {Primary, Controls, Stories, Meta, Description} from '@storybook/blocks';
import SideMenu from './SideMenu.vue';

import * as SideMenuStories from './SideMenu.stories.js';

<Meta of={SideMenuStories} />

# Side Menu

## Usage

This component is built on PrimeVue's `PanelMenu`, rendering a side menu with a list of items. It supports nesting, allowing items to expand and collapse for easy navigation through multiple levels.

<Primary />
<Controls />
<Stories />
