import {
	Primary,
	Controls,
	Stories,
	Meta,
	Description,
} from '@storybook/addon-docs/blocks';

import * as TopNavActionsStories from './TopNavActions.stories.js';

<Meta of={TopNavActionsStories} />

# TopNavActions

## Usage

The `TopNavActions` component provides action buttons for the top navigation of OJS, OMP and OPS. It is primarily used in the main backend **Smarty** template and **side modals** within the application.

<Controls />
<Stories />
