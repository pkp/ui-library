import {
	Primary,
	Controls,
	Stories,
	Meta,
	ArgTypes,
} from '@storybook/addon-docs/blocks';

import * as CheckboxStories from './Checkbox.stories.js';

<Meta of={CheckboxStories} />

# Checkbox

A custom-styled, accessible checkbox component with icon-based checked/unchecked states.

## Usage

Use this component to display a custom-styled checkbox that shows its state with an icon.

<Primary />
<ArgTypes />
