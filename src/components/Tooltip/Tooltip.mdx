import {
	Primary,
	Controls,
	Stories,
	Meta,
	ArgTypes,
} from '@storybook/addon-docs/blocks';

import * as TooltipStories from './Tooltip.stories.js';

<Meta of={TooltipStories} />

# Tooltip

## Usage

Use the tooltip when you want to provide short advice on how to complete a task. Do _not_ use the tooltip for information that the user requires to complete a task. Most users will not interact with the tooltip, so you should not rely on them reading the information.

<Primary />
<Controls />
