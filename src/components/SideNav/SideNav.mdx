import {Primary, Controls, Stories, Meta, Description} from '@storybook/blocks';
import SideNav from './SideNav.vue';

import * as SideNavStories from './SideNav.stories.js';

<Meta of={SideNavStories} />

# Side Nav

## Usage

The SideNav component serves as the main navigation container for OJS, OMP and OPS. It incorporates the `SideMenu` component, which is built on PrimeVue's `PanelMenu`.

<Primary />
<Controls />
<Stories />
