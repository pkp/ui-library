import {Primary, Controls, Stories, Meta, ArgTypes} from '@storybook/blocks';

import * as FileUploadProgress from './FileUploadProgress.stories.js';

<Meta of={FileUploadProgress} />

# FileUploadProgress

## Usage

Use this component to display a file upload in progress. Usually, this component is used with **FileUploader**.

<Primary />
<ArgTypes />
<Stories />
