import {Meta, ArgTypes} from '@storybook/blocks';

import * as WorkflowLogResponseForModalStories from './WorkflowLogResponseForModal.stories';

<Meta of={WorkflowLogResponseForModalStories} />

# Log Response Modal

A vue modal containing a form for and admin to log a response to a review request on behalf of the reviewer.

<ArgTypes/ >
